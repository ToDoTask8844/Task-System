# Task-System
To-Do List Application is a client-side web app built with HTML, CSS, and JavaScript that helps users manage daily tasks. It supports CRUD operations, task filtering, DOM manipulation, event handling, and localStorage for data persistence, ensuring tasks remain available after page refreshes.
